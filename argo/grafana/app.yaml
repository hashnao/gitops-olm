apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cluster-scoped-grafana-operator
  namespace: openshift-gitops
spec:
  destination:
    server: 'https://kubernetes.default.svc'
  info:
    - name: Description
      value: Deploy Grafana Operator on cluster-scoped basis
  project: default
  source:
    path: charts/grafana
    repoURL: 'https://github.com/hashnao/gitops-olm.git'
    targetRevision: main
